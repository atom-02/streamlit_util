diff --git a/README.md b/README.md
index 94ac9d1..6bed421 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # 문서 유틸리티 (Streamlit)
 
-두 가지 문서 도구를 한 화면에 모은 웹앱입니다. 상단 탭으로 전환합니다.
+세 가지 문서 도구를 한 화면에 모은 웹앱입니다. 상단 탭으로 전환합니다.
 
 ## 도구
 
@@ -19,6 +19,13 @@ LaTeX 수식이 포함된 마크다운(.md)을 한글(HWPX) 문서로 변환합
 - **하나로 합치기** — 지정한 순서대로 병합, 파일명 기준 북마크(목차) 자동 생성
 - 쪽 지정: `9-12`(범위), `1, 3, 5`(낱장), `1, 9-12, 20`(혼합)
 
+### 🔗 QR코드 안내문 생성기
+제목과 링크 주소를 입력하면 QR코드가 담긴 안내문을 PDF 또는 워드(.docx)로 만듭니다.
+
+- 제목·링크는 왼쪽 정렬, QR코드는 페이지 가로 중앙에 크게 배치
+- 내용 길이와 상관없이 위·아래 여백이 자동으로 균형 있게 맞춰짐
+- 워드 문서의 링크는 클릭 가능한 하이퍼링크로 삽입됨
+
 ## 로컬 실행
 
 ```bash
@@ -39,13 +46,15 @@ streamlit run streamlit_app.py
 ## 파일 구조
 
 ```
-streamlit_app.py    메인 진입점 (탭으로 두 도구 전환)
+streamlit_app.py    메인 진입점 (탭으로 세 도구 전환)
 md2hwpx_app.py      Markdown → HWPX 변환 로직 + UI (render())
 pdf_tool.py         PDF 추출·병합 로직 + UI (render())
-requirements.txt    streamlit, pypdf
+qr_tool.py          QR코드 안내문(PDF/워드) 생성 로직 + UI (render())
+requirements.txt    streamlit, pypdf, qrcode[pil], reportlab, python-docx
+packages.txt        fonts-nanum (PDF에 한글을 임베드하기 위한 apt 패키지)
 ```
 
-두 모듈 모두 `render()` 함수만 노출하며, `st.set_page_config()`는
+세 모듈 모두 `render()` 함수만 노출하며, `st.set_page_config()`는
 `streamlit_app.py`에서 한 번만 호출합니다.
 
 ## 참고
diff --git a/requirements.txt b/requirements.txt
index 13bb931..6e56452 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,2 +1,5 @@
 streamlit
 pypdf
+qrcode[pil]
+reportlab
+python-docx
diff --git a/streamlit_app.py b/streamlit_app.py
index d7efaaf..f1f20b3 100644
--- a/streamlit_app.py
+++ b/streamlit_app.py
@@ -1,14 +1,17 @@
-"""문서 유틸리티 - Markdown→HWPX 변환기 + PDF 쪽 추출·병합기"""
+"""문서 유틸리티 - Markdown→HWPX 변환기 + PDF 쪽 추출·병합기 + QR코드 안내문 생성기"""
 import streamlit as st
 
 st.set_page_config(page_title="문서 유틸리티", page_icon="🧰", layout="centered")
 
 from md2hwpx_app import render as render_md2hwpx
 from pdf_tool import render as render_pdf_tool
+from qr_tool import render as render_qr_tool
 
 st.title("🧰 문서 유틸리티")
 
-tab_md, tab_pdf = st.tabs(["📄 Markdown → HWPX", "📑 PDF 쪽 추출 · 병합"])
+tab_md, tab_pdf, tab_qr = st.tabs(
+    ["📄 Markdown → HWPX", "📑 PDF 쪽 추출 · 병합", "🔗 QR코드 안내문"]
+)
 
 with tab_md:
     render_md2hwpx()
@@ -16,5 +19,8 @@ with tab_md:
 with tab_pdf:
     render_pdf_tool()
 
+with tab_qr:
+    render_qr_tool()
+
 st.divider()
 st.caption("업로드한 파일은 서버에 저장되지 않고 세션 메모리에서만 처리됩니다.")
